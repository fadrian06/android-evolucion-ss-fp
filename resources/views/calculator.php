<?php /** @var ?float $rate */ ?>
<header class="page-head"><div><span class="eyebrow">HERRAMIENTA</span><h1>Calculadora</h1><p>Convierte con la cotización vigente.</p></div></header>
<div class="grid grid-2"><?php if ($rate !== null): ?><section class="card panel" x-data="{amount:'',direction:'usd',rate:<?= json_encode($rate) ?>}"><span class="eyebrow">TASA <?= strtoupper($source) ?></span><h2>Bs. <?= number_format($rate,2,',','.') ?> / USD</h2><div class="stack"><div class="field"><label>Monto</label><input x-model="amount" type="number" min="0" step="0.01" placeholder="0,00"></div><div class="field"><label>Conversión</label><select x-model="direction"><option value="usd">Dólares a bolívares</option><option value="ves">Bolívares a dólares</option></select></div><div class="metric card"><span>Resultado</span><strong x-text="amount === '' ? '—' : (direction === 'usd' ? 'Bs. ' + (amount*rate).toFixed(2) : '$' + (amount/rate).toFixed(2))"></strong></div></div></section><?php endif ?><section class="card panel"><span class="eyebrow">ADMINISTRACIÓN</span><h2>Cotización personalizada</h2><p>Esta tasa es necesaria para facturar hoy.</p><form method="post" action="./calculadora/cotizacion" class="stack"><div class="field"><label>Bolívares por dólar</label><input name="rate" type="number" min=".01" step=".000001" value="<?= $customRate?->rate ?? '' ?>" required placeholder="Cotización"></div><button class="btn btn-primary">Guardar cotización</button></form></section></div>
